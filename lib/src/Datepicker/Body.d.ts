import { Datepicker } from '../useDatepicker';
declare type Props = {
    datepicker: Datepicker;
};
declare const Body: ({ datepicker: { days, local, updates } }: Props) => JSX.Element;
export type { Props };
export default Body;
